//ReportingSection.js

import React from 'react';

const ReportingSection = ({ title }) => {
  return (
    <div className="vertical-section">
      <h2>{title}</h2>
      {/* Content for the vertical section */}
    </div>
  );
};

export default ReportingSection;
